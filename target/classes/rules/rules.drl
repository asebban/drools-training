package rules

import ma.medtech.drools.model.Person;
import ma.medtech.drools.model.Majeur;

rule "Insérer un majeur"
when
  $p : Person(age >= 18)
then
  System.out.println("[R1] " + $p.getName() + " est majeur.");
  insertLogical(new Majeur($p.getName()));
end

rule "Afficher les majeurs"
when
  $m : Majeur()
then
  System.out.println("[R2] " + $m + " est dans la base de faits (logique)");
end

package rules

import ma.medtech.drools.model.Counter;

rule "Incrémenter compteur"
when
  $c : Counter(value < 5)
then
  System.out.println("[R1] Incrementation de " + $c.getValue());
  $c.setValue($c.getValue() + 1);
  update($c);
end
